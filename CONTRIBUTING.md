# 贡献指南（CONTRIBUTING）

感谢你愿意花时间看看这个项目。我们规模很小，你的每一条反馈、建议和代码都很有帮助。在动手之前，先读一下这份说明，能让双方都少走弯路。

## 这个项目目前的状态

当前项目处于早期实验阶段：它在开发中基本能用，但没有经过完整、长期的测试，可能有不少问题。因此我们对"贡献"的定义很宽——不只是提 PR，发现 bug、吐槽文档、分享你的使用体验，都是很有价值的贡献。

## 环境准备

- Node.js 22.x（`>=22.0.0 <23.0.0`），npm 管理依赖
- `npm install` 安装依赖
- 本地开发：`npm run dev`（前端） + `npm run server`（Socket 服务端）
- 代码规范化脚本：`npm run typecheck` / `npm run lint` / `npm test` / `npm run test:integration` / `npm run test:e2e`

## 关于 AI 能力的说明

- AI 玩家依赖外部大模型服务，运行时需自行提供 API key，请通过环境变量注入，**不要把密钥写进代码或提交到仓库**。
- 没有 AI 配置时，AI 发言会不可用或退化，这不影响其他玩家对局的进行。

## 怎么提交改动

1. 提 PR 前，先开一个 issue 或 Discussion 说明你想做什么 / 发现了什么，避免撞车。
2. 分支从 `main` 拉出，提交信息尽量描述清楚"改了什么、为什么"。
3. 新功能尽量带上测试（前端单测、服务端集成测试或 e2e 之一即可，不强求全都要）。
4. 提交前至少跑 `npm run typecheck` 和对应模块的测试，确认没引入明显问题。
5. 描述改动时**如实**：别把"可能没测过"说成"完全没问题"；有问题就说有问题。

## 行为准则

- 对事不对人。代码有分歧很正常，拿理由说话，别攻击。
- 如实汇报：不确定的地方就说"不确定"，宁可明确说"没测过"，也不要含糊过关。
- 尊重用户隐私：不要在文档、issue、代码里粘贴任何真实账号、URL 或密钥。

## 联系我们

直接开 issue 即可。这个项目不追求大而全，你的每个反馈都会让它的下一步更稳一点。